version: '3'
services:

  #  app:
  #    build: .
  #    depends_on:
  #      - db

  db:
    image: postgres:13.3
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=admin
      - POSTGRES_DB=slurpbook
    ports:
      - "5432:5432"